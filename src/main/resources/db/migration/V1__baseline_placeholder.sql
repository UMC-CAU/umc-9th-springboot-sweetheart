-- Flyway Baseline Placeholder
-- 현재 DB 스키마가 이미 최신 상태이므로 이 파일은 플레이스홀더입니다.
-- baseline-on-migrate: true 설정으로 기존 스키마는 V0으로 인식됩니다.
--
-- 향후 스키마 변경 시 새로운 마이그레이션 파일을 추가하세요:
-- 예) V2__add_new_column.sql, V3__create_new_table.sql
--
-- 파일 명명 규칙:
-- V{버전}__{설명}.sql (언더스코어 2개 필수!)
-- 예) V2__add_profile_image_to_member.sql

SELECT 1;  -- 빈 파일 방지용 더미 쿼리 (Flyway는 빈 파일 에러 발생)
