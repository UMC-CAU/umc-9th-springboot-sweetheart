# ========================================
# 환경 변수 설정 예시 (Production 환경용)
# ========================================
#
# 📍 사용 방법:
#   1. 이 파일을 .env로 복사: cp .env.example .env
#   2. 아래 값들을 실제 값으로 변경
#   3. ⚠️ .env 파일은 절대 Git에 커밋하지 마세요!
#
# 📍 환경별 설정:
#   - Local: .env.local + run.sh 사용
#   - Prod: GitHub Secrets에서 자동 생성 (deploy.yml)
#
# 📍 GitHub Secrets 설정:
#   Repository → Settings → Secrets and variables → Actions
#   아래 변수들을 모두 등록하세요!
#

# ========================================
# Database Configuration
# ========================================
DB_ROOT_PASSWORD=change_this_root_password
DB_USER=umc_user
DB_PW=change_this_password

# ========================================
# Discord Webhook (에러 알림)
# ========================================
# Discord: Server Settings → Integrations → Webhooks
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR_WEBHOOK_ID/YOUR_WEBHOOK_TOKEN

# ========================================
# Google OAuth2 Configuration
# ========================================
# Google Cloud Console: https://console.cloud.google.com/apis/credentials
#
# ⚠️ Authorized redirect URIs에 아래 URL들을 모두 등록하세요:
#    - http://localhost:8080/login/oauth2/code/google (Local 테스트용)
#    - https://spring-swagger-api.log8.kr/login/oauth2/code/google (Production)
#
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here
GOOGLE_REDIRECT_URI=https://spring-swagger-api.log8.kr/login/oauth2/code/google

# ========================================
# OAuth2 Frontend Redirect
# ========================================
# 로그인 성공 시 JWT 토큰을 전달할 프론트엔드 URL
OAUTH2_REDIRECT_URL=https://your-frontend-domain.com/oauth/callback

# ========================================
# JWT Configuration
# mac에서 openssl rand -base64 48 실행 후 나온 값
# ========================================
JWT_SECRET_KEY=your_jwt_secret_key_here